name: 🚀 Auto Deploy to Spaceship Hosting (TheInstaViewer.com)

on:
  push:
    branches:
      - master  # ya 'main' agar aapki branch ka naam woh hai

jobs:
  ftp-deploy:
    name: Upload to TheInstaViewer.com folder
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Code checkout karo
        uses: actions/checkout@v2

      - name: 🚀 FTP se deploy karo
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.TheInstaViewer.com
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ./
